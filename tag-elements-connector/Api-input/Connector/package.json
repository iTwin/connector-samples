{
  "name": "@iTwin/tagging-connector",
  "version": "0.0.0",
  "description": "Connector to augment iModel with tagged elements.",
  "main": "lib/App.js",
  "scripts": {
    "start": "npm run build && node ./lib/App.js",
    "build": "tsc && npm run copyFiles",
    "copyFiles": "npx babel src --out-dir lib --copy-files",
    "clean": "rimraf lib"
  },
  "dependencies": {
    "@bentley/bentleyjs-core": "^2.19.6",
    "@bentley/process-functional-schema": "^1.0.1",
    "@itwin/pcf": "^0.3.0-3",
    "axios": "^0.27.2",
    "xlsx": "^0.17.0"
  },
  "devDependencies": {
    "@bentley/build-tools": "^2.16.0",
    "@types/node": "^10.17.28",
    "babel-cli": "^6.26.0",
    "typescript": "^4.1.0"
  }
}
